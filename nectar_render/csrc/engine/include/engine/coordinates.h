#pragma once

// class UVSample {
// public:
//     float u, v;

//     __host__ __device__ UVSample(int x, int y, int H, int W) {
//         u = ((float)x / (float)(W - 1));
//         v = ((float)y / (float)(H - 1));
//     }

// };

